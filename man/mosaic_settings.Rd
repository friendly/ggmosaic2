% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mosaic-settings.R
\name{mosaic_settings}
\alias{mosaic_settings}
\title{Settings for mosaic plot layers}
\usage{
mosaic_settings(divider, offset, expected, area)
}
\arguments{
\item{divider}{A divider function, a character vector naming divider
functions, or a list of divider functions.}

\item{offset}{A single non-negative number giving the gap at the deepest
split. Gaps increase by a factor of 1.5 toward the outermost split.}

\item{expected}{The log-linear model used to calculate expected frequencies
and Pearson residuals. Supply a formula or one of \code{"independence"},
\code{"saturated"}, or \code{"conditional"}. The conditional model requires one or
more variables mapped to \code{conds}. Supply \code{NULL} to turn off model fitting,
including a model set by an earlier call to \code{mosaic_settings()}.}

\item{area}{Values used to construct mosaic rectangles: \code{"observed"} (the
default) or fitted \code{"expected"} counts. Expected areas require a
non-\code{NULL} effective \code{expected} setting.}
}
\value{
An object of class \code{"ggmosaic_settings"} that can be added to a
ggplot with \code{+}.
}
\description{
Set the divider, gap size, model, and area basis for mosaic layers in a
plot. A value set directly in a layer takes priority.
}
\details{
A layer inherits a setting only when its corresponding argument is omitted.
Settings are resolved in this order: an explicitly supplied layer argument
(including \code{expected = NULL}), the plot's \code{mosaic_settings()} value, and the
layer default (\code{mosaic()}, \code{0.01}, \code{NULL}, or \code{"observed"}).

\code{divider} and \code{offset} are inherited by \code{\link[=geom_mosaic]{geom_mosaic()}}, \code{\link[=stat_mosaic]{stat_mosaic()}},
\code{\link[=geom_mosaic_text]{geom_mosaic_text()}}, \code{\link[=stat_mosaic_text]{stat_mosaic_text()}}, \code{\link[=geom_mosaic_jitter]{geom_mosaic_jitter()}}, and
\code{\link[=stat_mosaic_jitter]{stat_mosaic_jitter()}}. \code{expected} and \code{area} are inherited by all six
constructors. Jitter layers use \code{expected} only when their effective
\code{area} is \code{"expected"}.

The position of \code{mosaic_settings()} among the layers in a plot has no
effect. If it is added more than once, the last supplied value for each
setting is used; omitted arguments do not change earlier settings.
Settings are local to the plot and do not change package or session defaults.
They share configuration, not computations or fitted model objects, between
layers.
}
\examples{
data(titanic)

ggplot(titanic, aes(x = product(Class, Sex))) +
  mosaic_settings(
    expected = "independence",
    area = "expected",
    divider = c("vspine", "hspine"),
    offset = 0.005
  ) +
  geom_mosaic() +
  geom_mosaic_text(display_values = "residual") +
  scale_fill_residual() +
  theme_mosaic()

# An explicit layer argument overrides the plot setting. Here NULL turns
# model fitting off for the mosaic layer.
ggplot(titanic, aes(x = product(Class, Sex), fill = Survived)) +
  mosaic_settings(expected = "independence") +
  geom_mosaic(expected = NULL)

}
\seealso{
\code{\link[=geom_mosaic]{geom_mosaic()}}, \code{\link[=stat_mosaic]{stat_mosaic()}}, \code{\link[=geom_mosaic_text]{geom_mosaic_text()}},
\code{\link[=stat_mosaic_text]{stat_mosaic_text()}}, \code{\link[=geom_mosaic_jitter]{geom_mosaic_jitter()}}, \code{\link[=stat_mosaic_jitter]{stat_mosaic_jitter()}},
\code{\link[=mosaic]{mosaic()}}, and \code{\link[=ddecker]{ddecker()}}
}
\author{
Gavin Klorfine
}
